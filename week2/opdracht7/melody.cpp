#include "melody.hpp" 
void melody::play( note_player & p ){ 
    p.play( note{ note::C5,  125000 } ); 
    p.play( note{ note::D5,  125000 } ); 
    p.play( note{ note::F5,  125000 } ); 
    p.play( note{ note::D5,  125000 } ); 
    p.play( note{ note::A5,  125000 } ); 
    p.play( note{ 0,  125000 } ); 
    p.play( note{ 0,  62500 } ); 
    p.play( note{ note::A5,  note::dQ } ); 
    p.play( note{ 0,  125000 } ); 
    p.play( note{ note::G5,  note::dH } ); 
    p.play( note{ note::C5,  125000 } ); 
    p.play( note{ note::D5,  125000 } ); 
    p.play( note{ note::F5,  125000 } ); 
    p.play( note{ note::D5,  125000 } ); 
    p.play( note{ note::G5,  125000 } ); 
    p.play( note{ 0,  125000 } ); 
    p.play( note{ 0,  62500 } ); 
    p.play( note{ note::G5,  note::dQ } ); 
    p.play( note{ 0,  125000 } ); 
    p.play( note{ note::F5,  note::dQ } ); 
    p.play( note{ note::E5,  125000 } ); 
    p.play( note{ note::D5,  note::dQ } ); 
    p.play( note{ note::C5,  125000 } ); 
    p.play( note{ note::D5,  125000 } ); 
    p.play( note{ note::F5,  125000 } ); 
    p.play( note{ note::D5,  125000 } ); 
    p.play( note{ note::F5,  note::dH } ); 
    p.play( note{ note::G5,  note::dQ } ); 
    p.play( note{ note::E5,  note::dQ } ); 
    p.play( note{ note::D5,  125000 } ); 
    p.play( note{ note::C5,  note::dQ } ); 
    p.play( note{ 261,  note::dQ } ); 
    p.play( note{ note::C5,  note::dQ } ); 
    p.play( note{ note::G5,  note::dQ } ); 
    p.play( note{ 0,  note::dQ } ); 
    p.play( note{ note::F5,  note::dF } ); 
    p.play( note{ note::C5,  125000 } ); 
    p.play( note{ note::D5,  125000 } ); 
    p.play( note{ note::F5,  125000 } ); 
    p.play( note{ note::D5,  125000 } ); 
    p.play( note{ note::A5,  125000 } ); 
    p.play( note{ 0,  125000 } ); 
    p.play( note{ 0,  62500 } ); 
    p.play( note{ note::A5,  note::dQ } ); 
    p.play( note{ 0,  125000 } ); 
    p.play( note{ note::G5,  note::dH } ); 
    p.play( note{ note::C5,  125000 } ); 
    p.play( note{ note::D5,  125000 } ); 
    p.play( note{ note::F5,  125000 } ); 
    p.play( note{ note::D5,  125000 } ); 
    p.play( note{ 1046,  note::dH } ); 
    p.play( note{ note::E5,  note::dQ } ); 
    p.play( note{ note::F5,  note::dQ } ); 
    p.play( note{ note::E5,  125000 } ); 
    p.play( note{ note::D5,  note::dQ } ); 
    p.play( note{ note::C5,  125000 } ); 
    p.play( note{ note::D5,  125000 } ); 
    p.play( note{ note::F5,  125000 } ); 
    p.play( note{ note::D5,  125000 } ); 
    p.play( note{ note::F5,  note::dH } ); 
    p.play( note{ note::G5,  note::dQ } ); 
    p.play( note{ note::E5,  note::dQ } ); 
    p.play( note{ note::D5,  125000 } ); 
    p.play( note{ note::C5,  note::dQ } ); 
    p.play( note{ 261,  note::dQ } ); 
    p.play( note{ note::C5,  note::dQ } ); 
    p.play( note{ note::G5,  note::dQ } ); 
    p.play( note{ 0,  note::dQ } ); 
    p.play( note{ note::F5,  note::dF } ); 
} 
